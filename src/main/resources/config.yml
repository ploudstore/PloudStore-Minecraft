secret-key: "your-secret-key-here"

messages:
  # {slots} -> slots necessarios, {free} -> slots livres atuais
  not-enough-slots: "&c[Loja] Precisa de {slots} slot(s) livre(s) no inventario para receber a sua compra. Tem apenas {free}."
